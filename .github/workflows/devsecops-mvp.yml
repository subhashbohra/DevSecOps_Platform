name: AI DevSecOps Debug

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  devsecops-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Trivy scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          ignore-unfixed: true
          format: json
          output: trivy-results.json

      # ⬇️ Dump raw JSON so we know what Trivy is giving us
      - name: Dump raw Trivy results
        run: |
          echo "## 🔍 Raw Vulnerability Report" > report.md
          echo "" >> report.md
          cat trivy-results.json >> report.md
          cat report.md

      - name: Generate AI explanation
        id: ai_explain
        run: |
          content=$(cat trivy-results.json)
          if [ -z "$content" ]; then
            echo "ai_response=No vulnerabilities JSON found" >> $GITHUB_OUTPUT
          else
            prompt="Summarize this Trivy vulnerability scan JSON in plain English. Show package, CVE, severity, installed version, and fixed version:\n\n$content"
            response=$(curl -s https://api.openai.com/v1/chat/completions \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer ${{ secrets.OPENAI_API_KEY }}" \
              -d "{
                \"model\": \"gpt-4o-mini\",
                \"messages\": [{\"role\": \"user\", \"content\": \"$prompt\"}]
              }" | jq -r '.choices[0].message.content // "AI explanation not available"')
            
            echo "ai_response<<EOF" >> $GITHUB_OUTPUT
            echo "$response" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          fi

      - name: Post PR Comment
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: devsecops-scan
          message: |
            ## 🛡️ AI DevSecOps Scan Summary
            ✅ Dependency scan completed.

            ### 📊 Vulnerability Report
            ```
            $(cat report.md)
            ```

            ### 🤖 AI Explanation
            ${{ steps.ai_explain.outputs.ai_response }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
