jq '{
  version: "2.1.0",
  runs: [
    {
      tool: {
        driver: {
          name: "CodeQL",
          rules: (
            [(.runs[].tool.driver.rules[]? // empty),
             (.runs[].tool.extensions[].rules[]? // empty)]
            | add // empty array if nothing
          )
        }
      },
      results: .runs[0].results
    }
  ]
}' ./_codeql_scanned_result/java.sarif > ./_codeql_scanned_result/fixed.sarif
